#! /bin/zsh
# -*- mode: sh -*-
# Created 2015/03/23

# ls
alias l="ls"
alias la="ls -a"
alias ll="ls -l"
alias lla="la -la"

# vim
alias v="vim"

# shutdown
case "${OSTYPE}" in
    "cygwin")
        alias sd="shutdown -h now"
        ;;
esac
